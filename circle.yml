machine:
  php:
    version: 5.6.5
  environment:
    COMPOSER_DISCARD_CHANGES: true
general:
  artifacts:
    - "build/"
    - "tmp/"
dependencies:
  cache_directories:
    - "vendor"
    - "bin"
test:
  override:
    - ./bin/phpspec run --no-interaction


